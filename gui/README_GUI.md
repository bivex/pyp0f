# PyP0f GUI - Графический интерфейс для анализа сетевого трафика

Графический интерфейс на PyQt5 для просмотра сетевого трафика в реальном времени с использованием pyp0f.

## 🚀 Запуск

### Способ 1: Скрипт запуска
```bash
./run_gui.sh
```

### Способ 2: Ручной запуск
```bash
# Переходим в директорию GUI
cd pyp0f/gui

# Активируем виртуальное окружение
source ../venv/bin/activate

# Запускаем с правами root
sudo python3 pyp0f_gui.py
```

## 📋 Требования

- **PyQt5** - установлен автоматически
- **pyp0f** - установлен
- **Права root** - для захвата сетевых пакетов
- **macOS** - интерфейс протестирован на macOS

## 🎯 Возможности

### 🎨 **Светлая тема по умолчанию**
- Современный, чистый интерфейс в стиле macOS
- Градиентные кнопки управления с цветовой кодировкой
- Чередование строк в таблице для лучшей читаемости
- Цветовая раскраска пакетов по типам:
  - 🟢 Зеленый: SYN пакеты (исходящие соединения)
  - 🟠 Желтый: SYN+ACK пакеты (входящие ответы)
  - 🔴 Красный: пакеты с ошибками анализа
- **Graceful shutdown**: Корректное завершение работы с подтверждением выхода

### 🔍 Анализ трафика в реальном времени:
- Захват TCP SYN и SYN+ACK пакетов
- Определение операционной системы отправителя
- Расчет расстояния до хоста (TTL анализ)
- Определение типа сетевого подключения (MTU)

### 📊 Интерфейс:
- **Таблица пакетов** - все захваченные пакеты с анализом
- **Статистика** - сводка по ОС, типам пакетов, последним пакетам
- **Лог событий** - сообщения о работе программы
- **Управление** - кнопки запуска/остановки захвата, очистки данных, копирования в Markdown

### 🎨 Визуальные особенности:
- Раскраска строк по типу пакета (зеленый/оранжевый/красный)
- Автопрокрутка таблиц
- Обновление статистики в реальном времени
- Статус-бар с информацией о работе

## 📖 Использование

### 1. Запуск захвата
- Нажмите кнопку **"▶️ Начать захват"**
- Программа начнет захватывать TCP пакеты
- Индикатор прогресса покажет активность

### 2. Просмотр результатов
- **Таблица пакетов** показывает все захваченные пакеты
- Каждый пакет анализируется на:
  - Время захвата
  - Тип пакета (SYN/SYN+ACK)
  - IP адреса и порты
  - Определенная ОС
  - Расстояние (distance)
  - Тип MTU

### 3. Статистика
- Автоматически обновляется каждую секунду
- Показывает распределение ОС
- Счетчики типов пакетов
- Последние 5 пакетов

### 4. Остановка захвата
- Нажмите **"⏹️ Остановить"**
- Захват прекратится, но данные останутся

### 5. Очистка данных
- Кнопка **"🗑️ Очистить"** удалит все данные

### 6. Копирование отчета
- Кнопка **"📋 Копировать в Markdown"** создаст полный отчет в формате Markdown
- Отчет включает:
  - Заголовок с датой и временем
  - Статистику операционных систем
  - Таблицу всех захваченных пакетов
  - Детальную статистику по типам пакетов
  - Лог событий
- Отчет копируется в буфер обмена для вставки в документы, отчеты или сообщения

### 7. Корректное завершение работы (Graceful Shutdown)
- **Закрытие окна**: При попытке закрыть окно во время активного захвата показывается диалог подтверждения
- **Обработка сигналов**: Корректная обработка SIGINT (Ctrl+C) и SIGTERM
- **Очистка ресурсов**: Автоматическая остановка потоков захвата и таймеров
- **Сохранение логов**: Финальная статистика записывается в лог перед выходом
- **Таймауты**: Безопасные таймауты для завершения операций

## 📊 Пример результатов

```
📦 Пакет от GitHub:
- Время: 14:23:15
- Тип: SYN+ACK
- Источник: 140.82.121.4:443
- ОС: Linux (Ubuntu)
- Дистанция: 12
- MTU: Google
```

## 🔧 Настройка

### Изменение интерфейса захвата
В файле `pyp0f_gui.py` найдите:
```python
self.capture_thread = PacketCaptureThread(interface='en0')
```
Замените `'en0'` на ваш сетевой интерфейс.

### Просмотр доступных интерфейсов
```bash
ifconfig  # или ip addr (на Linux)
```

## 🐛 Устранение проблем

### "Permission denied" при захвате
```bash
# Запустите с sudo
sudo python3 pyp0f_gui.py
```

### Пустая таблица пакетов
- Проверьте, что есть сетевой трафик
- Откройте браузер и загрузите веб-страницу
- Или выполните: `ping google.com`

### Ошибка загрузки базы данных
```bash
# Переходим в директорию GUI и активируем виртуальное окружение
cd pyp0f/gui
source ../venv/bin/activate
```

## 📁 Структура файлов

```
├── pyp0f_gui.py      # Основной файл GUI
├── run_gui.sh        # Скрипт запуска
├── test.py           # Консольная версия
├── analyze_pcap.py   # Анализатор pcap файлов
└── my_custom_db.fp   # Пример пользовательской базы
```

## 🎨 Скриншоты интерфейса

### Главное окно:
- Верхняя панель: кнопки управления
- Центральная область: таблица захваченных пакетов
- Нижняя область: статистика и лог

### Цветовая схема:
- 🟢 Зеленый: исходящие SYN пакеты
- 🟠 Оранжевый: входящие SYN+ACK пакеты
- 🔴 Красный: ошибки анализа

## 🔒 Безопасность

- Приложение требует права root только для захвата пакетов
- Не сохраняет пароли или конфиденциальные данные
- Работает только на localhost (GUI)

## 📞 Поддержка

При проблемах:
1. Проверьте логи в нижней панели
2. Убедитесь, что pyp0f установлен правильно
3. Проверьте права доступа к сети

---

**Приятного анализа сетевого трафика!** 🛡️🔍
